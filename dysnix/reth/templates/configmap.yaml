apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "reth.fullname" . }}-config
  labels:
    {{- include "reth.labels" . | nindent 4 }}
data:
  reth.toml: |-
    {{- include (print $.Template.BasePath "/_config.tpl") . | nindent 4 }}
