apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "gcp-local-nvme-raid.fullname" . }}-test-connection"
  labels:
    {{- include "gcp-local-nvme-raid.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "gcp-local-nvme-raid.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
